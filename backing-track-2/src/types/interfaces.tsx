export interface Beat {
    url: string;
    id?: string;
    title?: string;
    keyCenter?: string;
    modality?: string;
    bpm?: number;
    thumbnailUrl?: string;
}

/*
export interface Playlist {
  id: string;
  name: string;
  description: string;
  image: string;
  tracks: Track[];
}

export interface Track {
  id: string;
  name: string;
  artist: string;
  album: string;
}
  */